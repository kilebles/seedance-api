services:
  frontend-next:
    build:
      context: frontend-next
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_API_BASE: "http://89.127.192.164:8000"
    ports:
      - "3000:3000"
    depends_on:
      - api
    restart: unless-stopped

  frontend:
    build:
      context: frontend
      dockerfile: Dockerfile
    ports:
      - "8501:8501"
    depends_on:
      - api
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./output:/app/output

  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file: .env
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped
    volumes:
      - logs:/app/logs
      - ./output:/app/output

  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: seedance
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d seedance"]
      interval: 5s
      timeout: 5s
      retries: 10
    restart: unless-stopped

volumes:
  postgres_data:
  logs:
